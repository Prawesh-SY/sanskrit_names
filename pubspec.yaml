
name: sanskrit_names
description: "A new Flutter package containing a list of Sanskrit names for children."
version: 0.0.1
homepage:

repository: https://github.com/Prawesh-SY/sanskrit_names

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

